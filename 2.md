我会按这份计划的核心思想做，但不会照单全收。当前 Ragent 已经有 /rag/eval 和 RagTrace，所以实现重点不是“从零造一套庞大平台”，而是把现有评测接口升级成“评测集 + 多次执行 + 自动打分 + Trace 归因 + 报告”的闭环。

  我会分四层落地。

  第一层：评测集管理

  先做数据模型和基础 API。表我会按项目现有风格改造，不直接用草案里的 BIGINT，而是用项目统一的雪花 ID 字符串、create_time/update_time/deleted 字段。

  核心表大概是：

  t_eval_suite        -- 评测套件，比如「知识库核心问答」「MCP工具调用」「多轮记忆」
  t_eval_case         -- 单条评测题
  t_eval_run          -- 一次评测任务
  t_eval_trial        -- 某个 case 的第 N 次执行
  t_eval_score        -- 每次 trial 的各项评分
  t_eval_report       -- 汇总报告/版本对比结果

  EvalCase 里最关键的是 ground_truth，我会先用 JSON 存，避免一开始模型过度复杂：

  {
    "referenceDocIds": ["FAQ_VAC_001"],
    "expectedIntentLeafIds": ["intent_leave_policy"],
    "expectedTool": {
      "name": "sales_query",
      "args": {
        "region": "华东"
      }
    },
    "requiredFacts": ["年假按司龄递增", "未休年假可结转一次"],
    "forbiddenClaims": ["年假可以无限结转"],
    "expectedAnswer": "可选，用于语义评估"
  }

  后台接口放在 /admin/eval/** 更合适，而不是继续堆在 /rag/eval 上。比如：

  GET    /admin/eval/suites
  POST   /admin/eval/suites
  POST   /admin/eval/suites/{suiteId}/cases/import
  GET    /admin/eval/cases
  POST   /admin/eval/runs
  GET    /admin/eval/runs/{runId}
  GET    /admin/eval/runs/{runId}/report

  现有 /rag/eval 我会保留，作为“单题调试接口”。真正批量评估走新的 EvalExecutionEngine。

  第二层：执行引擎

  这里复用当前 /rag/eval 背后的能力，但不要通过 HTTP 自己调自己，而是抽一个内部 runner。

  现在 bootstrap/src/main/java/com/nageoffer/ai/ragent/rag/eval/EvalController.java:50 已经能跑：

  question -> rewrite -> intent -> retrieval

  我会把这段逻辑抽成类似：

  EvalRagRunner.run(question)

  返回一个结构化快照：

  EvalSnapshot {
      originalQuestion
      rewrittenQuestion
      subQuestions
      intentLeafIds
      retrievedDocIds
      retrievedChunkIds
      retrievedContexts
      retrievedContextDocIds
      mcpToolName
      mcpArgs
      mcpContext
      answer
      traceId
      latencyMs
  }

  第一版可以只跑到检索，不生成最终答案；第二版再接入完整回答生成。这样可以分阶段交付：

  - MVP：评估意图识别 + 文档召回 + chunk 质量
  - 增强版：评估最终回答事实性、引用质量、幻觉

  trial 这块我会做成可配置：

  eval:
    execution:
      max-concurrency: 2
      default-trial-count: 5
      timeout-seconds: 120

  每个 case 跑多次，写入 t_eval_trial。每次 trial 都保存 trace_id，这样失败后可以直接跳到当前已有 Trace 页面看细节。

  第三层：自动阅卷器

  我不会第一步就硬集成 Ragas。Ragas 是好东西，但 Python 服务会引入部署、鉴权、稳定性和数据传输成本。当前项目先做 Java 原生 scorer，收益更快，也更贴合现有系统。

  我会定义统一接口：

  public interface EvalScorer {
      List<EvalMetricResult> score(EvalCase evalCase, EvalSnapshot snapshot);
  }

  然后用 Spring Bean 自动装配多个 scorer：

  1. IntentAccuracyScorer
     对比 intentLeafIds 和 expectedIntentLeafIds，算 top1/topk 准确率。

  2. RetrievalDocRecallScorer
     对比 retrievedDocIds 和 referenceDocIds，算 recall@k。

  3. RetrievalContextPrecisionScorer
     用 retrievedContextDocIds 和 referenceDocIds 算 chunk 级 precision。

  4. McpToolScorer
     判断 MCP 工具是否选对。

  5. McpArgsScorer
     判断工具参数是否正确，先支持 exact match，后面再支持容忍规则。

  6. AnswerRuleScorer
     检查最终答案是否包含 requiredFacts，是否出现 forbiddenClaims。

  7. LLMJudgeScorer
     第二阶段再上。用项目现有模型路由能力，不硬编码 GPT-4/DeepSeek。Judge prompt 固化版本，输出 JSON 分数和理由。

  分数结构类似：
    "metric": "retrieval_doc_recall",
    "score": 0.8,

  - success_rate_at_k：5 次里成功 4 次就是 80%
  - pass_at_k：5 次里至少 1 次成功就算这个 case 被 pass

  上线门禁更适合看 success_rate_at_k，因为它能反映稳定性。

  第四层：报告和可视化

  前端我会新增一个后台模块：评估中心。

  页面分四块：

  评测套件列表
  评测用例管理
  评测运行记录
  评测报告详情

  报告详情里重点展示：

  - 总通过率
  - Pass@K / success_rate@K
  - 意图准确率
  - 检索 recall@k / precision@k
  - MCP 工具准确率
  - 答案质量分
  - 延迟 p50/p95
  - 失败 case 列表
  - 每个失败 case 的 Trace 链接
  - baseline vs current 对比

  这里可以直接复用当前 Trace 页面。我的思路是：评估报告负责告诉你“哪题坏了”，Trace 负责告诉你“为什么坏了”。

  我会调整这份计划里的几个点

  第一，思维链可视化 这个说法要改。生产系统不应该采集或展示模型隐藏 chain-of-thought。我会展示“结构化执行轨迹”：改写结果、意图候选、召回 chunk、MCP 参数、模型输出、评分理由。这足够归因，也更安全。

  第二，Ragas 不作为第一阶段 P0。我会先实现本项目自己能稳定跑的指标：意图准确率、召回率、MCP 准确率、规则答案评分。Ragas 后面作为 RagasScorerAdapter 接进去。

  第三，报告生成不一定上 FreeMarker。项目已有 prompt 模板习惯，前端也能渲染报告。第一版我会后端生成 Markdown，前端展示；HTML 报告后置。

  第四，评估任务必须限流。不能让 50 题 × 5 次评估直接把线上模型、向量库、MCP 工具打爆。默认并发我会设成 1 或 2，并支持取消任务。

  实际交付顺序

  我会按这个顺序做：

  1. 新增评测集/用例/运行/评分表和实体。
  2. 把现有 /rag/eval 的核心逻辑抽成 EvalRagRunner。
  3. 做 EvalExecutionEngine，支持一个 suite 批量跑、多 trial、保存结果。
  4. 实现第一批 scorer：意图、召回、MCP、延迟。
  5. 做后台 API：创建评测任务、查看结果、失败详情。
  6. 做前端评估中心基础页。
  7. 再补 LLM-as-Judge 和报告版本对比。
  8. 最后才考虑 Ragas 或 Python 侧集成。

  第一版的目标不是“大而全”，而是先做到这件事：每次改 prompt、改 chunk 策略、改模型配置后，可以点一下运行评测，10 分钟内知道有没有把核心场景搞坏。这才是当前项目最需要的工程闭环。